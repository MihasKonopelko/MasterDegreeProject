// Some methods in a header file
class CharacterController
{
public:
	// Some public variables and methods.
	
private:

	void processMouseEvents(Event mouseEvent);
	
	void processKeyUpEvents(Event t_keyboard_event);
	
	void processKeyDownEvents(Event t_keyboardEvent);
								
};