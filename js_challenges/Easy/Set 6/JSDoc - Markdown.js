/**
 * Collection of data to represent a square.
 * @class
 * Defines a square position and size.  
 * Contains:
 *   the coordinates on X and Y position.
 *   the width and height.
 */
class Square extends Shape {
	// Inner class logic.
}

/**
 * Collection of data to represent a circle.
 * @class
 * Defines a circle position and radius.  
 * Contains:
 *   - the coordinates on X and Y position.
 *   - the radius of the circle.
 */
class Circle extends Shape {
	// Inner class logic.
}

/**
 * Collection of data to represent a triangle.
 * @class
 * Defines a triangle position and verticies.  
 * Contains:
 *   * the coordinates on X and Y position.
 *   * the array of 3 verticies.
 */
class Triangle extends Shape {
	// Inner class logic.
}

