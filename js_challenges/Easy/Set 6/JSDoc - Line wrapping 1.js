/** 
 * Returns a random shape in a specified position
 * within the screen position. 
 * @param {number} xPos Represents a position on the
 * horizontal axis from 0 to a screen width.
 * @param {number} yPos Represents a position on the
 * vertical axis from 0 to a screen height.
 */
function getRandomShape(xPos, yPos) {
	// Inner Logic.
}

/** Returns a random number in range from min to max. 
 * @param {number} min It is a minimal value which 
 *     can be selected by a random number generator.
 * @param {number} max It is a maximal value which 
 *     can be selected by a random number generator.
 */
function getRandomNumber(min, max) {
	// Inner Logic.
}