/** 
 * Returns a random circle of specified radius and position. 
 * @param {number} xPos @param {number} yPos
 * @param {number} radius
 */
function getRandomCircle(xPos, yPos, radius) {
	// Inner Logic.
}

/** 
 * Returns a result of substraction. 
 * @param {number} value1
 * @param {number} value2
 */
function substract(value1, value2) {
	// Inner Logic.
}

/** Returns a random number in range from min to max. 
 * @param {number} min @param {number} max
 */
function getRandomNumber(min, max) {
	// Inner Logic.
}