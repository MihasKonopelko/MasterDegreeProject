/**
 * A message handler which interprets the 
 * messages from the python server.
 */
class MessageHandler {
	// Inner Logic.
}

/**
 * This class parses message into a JSON
 * and posts it to the python server.
 */
class MessageSender {
	// Inner Logic.
}


class Message {	
	// Inner Logic.
}