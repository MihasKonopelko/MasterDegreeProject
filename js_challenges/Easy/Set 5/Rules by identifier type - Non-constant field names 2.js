class Rectangle {
	constructor(x, y, width, height) {
		/** @private @let {!Vector2} */
		this.position_ = new Vector2(x, y);
		/** @private @let {!number} */
		this.width = width;
		/** @private @let {!number} */
		this.height = height;
	}

}