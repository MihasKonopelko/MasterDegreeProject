/**
 * @param {number} gold How much gold the treasure will have.
 * @param {number=} artifactID The artifact which player gets.
 */
function generateTreasure(gold, artifactID = -1) {
	// Inner logic.
}

/**
 * @param {number} merchantType What the merchant sells (potions or weapons, ect).
 * @param {number} level The level of items merchant sells.
 * @param {number} hasMagicalItems Does merchant sell magical items.
 */
function generateMerchant(merchantType, level = 1, hasMagicalItems = -1) {
	// Inner logic.
}