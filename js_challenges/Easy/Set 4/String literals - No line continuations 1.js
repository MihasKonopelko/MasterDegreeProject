const stringOne = 'This is an overly long string of text \
		which has no reason to exist';
		
const stringTwo ='This is a long string too, however it is ' +
		'split differently to the string above... case is which ' + 
		'one is a correct way?';